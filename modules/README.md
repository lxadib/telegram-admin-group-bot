# Domain modules

Each subdirectory is a Composer path package (`telegram-platform/module-*`) that depends only on `telegram-platform/contracts`.

Modules are registered in Phase 3+ via manifests and `config/platform.php`.
